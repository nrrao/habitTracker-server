CREATE TABLE habits_users (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  user_name TEXT NOT NULL UNIQUE,
  password TEXT NOT NULL,
 );

ALTER TABLE habits
  ADD COLUMN
    user_id INTEGER REFERENCES habits_users(id)
    ON DELETE SET NULL;
